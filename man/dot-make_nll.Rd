% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{.make_nll}
\alias{.make_nll}
\title{Build the joint negative log-likelihood}
\usage{
.make_nll(design, family, y, fx = list(), w = NULL, obs = TRUE)
}
\arguments{
\item{design}{From \code{\link[=.build_design]{.build_design()}}.}

\item{family}{A \code{gamRTMB_family}.}

\item{y}{Response.}

\item{fx}{Resolved fixed arguments.}

\item{w}{Prior weights, or \code{NULL} for unweighted.}

\item{obs}{Mark the response with \code{\link[RTMB:TMB-interface]{RTMB::OBS()}}, so that \code{obj$simulate()}
and one-step-ahead residuals can substitute for it. Only the Laplace
engine builds the object those need, and \code{OBS()} is not free of
consequences elsewhere: it keys on the \emph{deparsed name} of its argument
in a registry global to \pkg{RTMB}, which \code{\link[RTMB:TMB-interface]{RTMB::MakeADFun()}} resets per
object but \code{\link[RTMB:Tape]{RTMB::MakeTape()}} does not. A tape built the second way
leaves \code{"y"} in that registry, and the next model's \code{OBS(y)} -- on a tape
or in plain R -- silently gets the previous model's response. So the EFS
engine, which taping is all it does, asks for \code{obs = FALSE}.}
}
\value{
A function of a parameter list \code{list(beta, b, log_sigma)}.
}
\description{
The penalized coefficients carry a mean-zero Gaussian prior, one variance
per penalized block. Which Gaussian depends on the route the block took
through \code{\link[=.build_design]{.build_design()}}: after \code{\link[mgcv:smooth2random]{mgcv::smooth2random()}} the penalty is the
identity and the prior is iid \eqn{N(0, \sigma_k^2)}, while a block that
kept its own sparse penalty gets \eqn{N(0, \sigma_k^2 Q_k^{-1})} through
\code{\link[RTMB:MVgauss]{RTMB::dgmrf()}}. The two are the same model written two ways; keeping
\eqn{Q_k} sparse is what lets a Markov random field over many regions stay
affordable, since \code{smooth2random}'s rotation would fill it in.
}
\details{
Null-space coefficients live in \code{beta} and are never given a prior.
Nothing is shared across smooths or across distributional parameters
unless an \code{id} says so.

Prior weights multiply each observation's log-density contribution, as in
\code{\link[stats:glm]{stats::glm()}}. Offsets are added to the relevant parameter's linear
predictor.
}
\keyword{internal}
