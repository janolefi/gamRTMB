% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{fam}
\alias{fam}
\alias{print.gamRTMB_family}
\title{Build a family object}
\usage{
fam(dist, links = NULL, fixed = NULL, start = NULL, eta_scale = NULL)

\method{print}{gamRTMB_family}(x, ...)
}
\arguments{
\item{dist}{Density name, with or without the leading \code{d}
(\code{"skewnorm2"} or \code{"dskewnorm2"}).}

\item{links}{Named character vector overriding the derived links.}

\item{fixed}{Named list of values for fixed arguments; each is a constant
or the name of a column of the data.}

\item{start, eta_scale}{Optional replacements for the starting-value and
linear-predictor-scale heuristics.}

\item{x}{A \code{gamRTMB_family}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{gamRTMB_family}.
}
\description{
Reads a density's \code{formals()} and derives everything needed to model it:
the native parameter names in the density's own order, a link per
parameter, which arguments are data rather than parameters, and starting
values. Most of RTMBdist works with no hand-written family; use
\code{\link[=families]{families()}} to see what is available.
}
\section{Where densities come from}{

\pkg{RTMBdist} is searched first, then a curated list of the standard
densities that \pkg{RTMB} makes AD-aware (\code{norm}, \code{pois}, \code{binom}, \code{gamma},
\code{exp}, \code{lnorm}, \code{weibull}, \code{cauchy}, \code{logis}, \code{t}, \code{chisq}). Parameter
names are always the density's own: a skew normal is \code{xi}, \code{omega},
\code{alpha}, and a Gaussian is \code{mean}, \code{sd}.
}

\section{Modelled versus fixed arguments}{

Modelled parameters get a formula, a link and smooths. Fixed arguments are
known data or constants — the number of binomial trials, truncation bounds,
a numerical \code{eps} — and are passed straight to the density, unreachable
from the formula interface. Supply one with \code{fixed = list(size = "trials")}
naming a column of the data, or a constant.
}

\section{Starting values}{

Location parameters get data-driven starts and the rest keep the density's
own default, with one exception. A shape parameter entering an already
mean/sd standardised density can have an \strong{identically zero score} at
the symmetric point: in \code{dskewnorm2} the direct effect of \code{alpha} on the log
density cancels exactly against the shift in the internal location needed
to hold \code{mean} and \code{sd} fixed, so the derivative is zero for every
observation at \code{alpha = 0} (measured at ~1e-15, i.e. exactly zero). The
default \code{alpha = 0} is therefore a perfectly neutral value and a useless
starting point: the optimiser has no descent direction, and because that
coefficient block has no curvature either, the inner Newton solve is
singular and the Laplace approximation is undefined. Such parameters start
off the symmetric point instead, with the sign taken from the sample
skewness — starting at \code{-0.5} on right-skewed data gets stuck just as badly
as starting at \code{0}.
}

\examples{
fam("norm")
fam("gamma2")
fam("skewnorm2")
fam("betabinom", fixed = list(size = "trials"))
}
\seealso{
\code{\link[=families]{families()}} for what is available, \code{\link[=gamRTMB]{gamRTMB()}} to fit.
}
