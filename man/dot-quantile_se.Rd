% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{.quantile_se}
\alias{.quantile_se}
\title{Delta-method standard errors for fitted quantiles}
\usage{
.quantile_se(object, prob, forms, Vj, fx = object$fixed, h = 1e-04)
}
\arguments{
\item{object}{A \code{gamRTMB} fit.}

\item{prob}{Probabilities.}

\item{forms}{Per-parameter linear forms from \code{\link[=predict.gamRTMB]{predict.gamRTMB()}}.}

\item{Vj}{Joint coefficient covariance from \code{\link[=.joint_cov]{.joint_cov()}}.}

\item{fx}{Resolved fixed arguments for the rows being predicted.}

\item{h}{Step for the central difference, on the link scale.}
}
\value{
A matrix of standard errors, one column per probability.
}
\description{
A quantile is a smooth function of every linear predictor at once, so its
variance needs the predictors' \emph{joint} covariance, cross-parameter
terms included: \eqn{Var(q) = \sum_{k,l} (\partial q / \partial \eta_k)
(\partial q / \partial \eta_l) Cov(\eta_k, \eta_l)}.
}
\details{
The derivatives are taken by central difference on the family's quantile
function, which avoids needing an analytic derivative for each of the
families that has one. The covariances come from the same linear forms the
per-parameter standard errors use, so \code{Cov(eta_k, eta_l)} is one row-wise
product per pair.
}
\keyword{internal}
