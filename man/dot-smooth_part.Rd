% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{.smooth_part}
\alias{.smooth_part}
\title{One smooth's design in joint-coefficient space}
\usage{
.smooth_part(fit, p, j, X)
}
\arguments{
\item{fit}{A \code{gamRTMB} fit.}

\item{p, j}{Distributional parameter and smooth index.}

\item{X}{The smooth's model matrix, from the fit or from
\code{\link[mgcv:smoothCon]{mgcv::PredictMat()}}.}
}
\value{
\code{list(Z, coef, b, f)}: the mapped design, the coefficients it
multiplies, and the \code{b} and \code{beta} indices for locating them in a
covariance matrix.
}
\description{
A smooth's fitted values and their standard errors are both linear forms in
the joint coefficient vector once its model matrix has been mapped through
the reparameterisation: with \verb{Z = X Tmap}, the contribution is \verb{Z c} and its
covariance \verb{Z V Z'}. Mapping the design once is simpler than mapping the
coefficients and their covariance separately, and it removes the need to
assemble a block-diagonal transform for the whole linear predictor.
}
\keyword{internal}
