% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{.flat_start}
\alias{.flat_start}
\title{Detect a distributional parameter that starts at a useless stationary point}
\usage{
.flat_start(grad, eval_at, pfull, is_beta, design)
}
\arguments{
\item{grad}{Joint gradient at the starting values.}

\item{eval_at}{Function of a full parameter vector returning the objective.}

\item{pfull}{The full starting parameter vector.}

\item{is_beta}{Logical index of the \code{beta} entries within \code{pfull}.}

\item{design}{The design object.}
}
\value{
A message describing the affected parameters, or \code{NULL}.
}
\description{
A parameter whose intercept has an identically zero score cannot move, and
a smooth on it then drifts on a flat surface instead of failing loudly.
\code{dskewnorm2}'s \code{alpha} does this at \code{alpha = 0}; see \code{\link[=fam]{fam()}}.
}
\details{
A zero score is not on its own a problem: an intercept started at its own
marginal optimum has one too, and that is exactly where it should be
(\code{dnbinom2}'s \code{mu} started at \code{log(mean(y))} has a zero score and fits
perfectly). The two are told apart by probing rather than by curvature —
perturb the intercept either way and see whether the objective actually
falls. A stationary point that can be improved on by stepping away from it
is the bad kind.
}
\keyword{internal}
