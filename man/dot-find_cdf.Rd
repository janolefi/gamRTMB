% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{.find_cdf}
\alias{.find_cdf}
\title{Locate the CDF matching a density}
\usage{
.find_cdf(dist, source, modelled)
}
\description{
\verb{p<dist>} in the density's own namespace, else in \pkg{stats}. Residuals
are computed after fitting, on plain numerics, so the CDF never has to be
AD-compatible.
}
\details{
A few RTMBdist CDFs take extra arguments the density does not (\code{ncp},
\code{method}, \code{from}, \code{tol}), which is harmless. The case that matters is the
reverse: if a modelled parameter is missing from the CDF's arguments it
cannot be evaluated faithfully, so no CDF is offered rather than one with a
parameter silently dropped.
}
\keyword{internal}
