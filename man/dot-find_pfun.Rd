% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{.find_pfun}
\alias{.find_pfun}
\title{Locate the CDF or quantile function matching a density}
\usage{
.find_pfun(prefix, dist, source, modelled)
}
\arguments{
\item{prefix}{\code{"p"} for the CDF, \code{"q"} for the quantile function.}

\item{dist, source}{Density name and the package it came from.}

\item{modelled}{The modelled parameter names.}
}
\value{
A function of \verb{(x, theta, fx)}, or \code{NULL}.
}
\description{
\verb{p<dist>} or \verb{q<dist>} in the density's own namespace, else in \pkg{stats}.
Neither has to be AD-compatible: residuals and quantiles are computed after
fitting, on plain numerics.
}
\details{
A few RTMBdist versions take extra arguments the density does not (\code{ncp},
\code{method}, \code{from}, \code{tol}), which is harmless. The case that matters is the
reverse: if a modelled parameter is missing from the arguments the function
cannot be evaluated faithfully, so nothing is offered rather than something
with a parameter silently dropped.
}
\keyword{internal}
