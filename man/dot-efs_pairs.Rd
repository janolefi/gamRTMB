% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efs.R
\name{.efs_pairs}
\alias{.efs_pairs}
\title{The exponent map from free smoothing parameters to penalty weights}
\usage{
.efs_pairs(design)
}
\value{
\code{list(pairs, Jac, group, nfree)}: one row of \code{pairs} per (block,
penalty) pair, \code{Jac[row, l] = da_row / drho_l}, and \code{group[m]} the free
parameter that entry \code{m} of \code{log_sigma} belongs to.
}
\description{
Collects every (block, penalty matrix) pair in the design into one table,
and records how each pair's exponent \eqn{a_{ki}} depends on the \emph{free}
parameter vector -- free meaning after \code{id}-tying has collapsed several
entries of \code{log_sigma} into one, which \code{\link[=.build_design]{.build_design()}} records in
\code{sig_group}.
}
\keyword{internal}
