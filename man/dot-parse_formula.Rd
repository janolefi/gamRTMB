% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design.R
\name{.parse_formula}
\alias{.parse_formula}
\title{Parse a distributional model formula}
\usage{
.parse_formula(formula, parnames)
}
\arguments{
\item{formula}{e.g. \code{y ~ list(mean = ~ s(x1) + s(x2), sd = ~ s(x1))}.}

\item{parnames}{Character vector of the family's modelled parameters.}
}
\value{
A list with the response expression and one formula per parameter,
ordered as \code{parnames}.
}
\description{
The response is taken from the left-hand side of the outer formula and each
distributional parameter gets a one-sided formula from the \code{list()} on the
right, in the style of \code{brms::bf()}. Any parameter the family declares but
the user does not mention is given \code{~1}, which is why the family spec has
to exist before formula processing.
}
\keyword{internal}
