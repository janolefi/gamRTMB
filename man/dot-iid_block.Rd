% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmrf.R
\name{.iid_block}
\alias{.iid_block}
\title{Build an iid block from a smooth, the \code{smooth2random} way}
\usage{
.iid_block(sm)
}
\arguments{
\item{sm}{A \code{smoothCon} object built with \code{absorb.cons = TRUE}.}
}
\description{
The other half of \code{\link[=.gmrf_block]{.gmrf_block()}}, with the same shape of answer, so that
\code{\link[=.build_design]{.build_design()}} can pick a route and then stop caring which it picked.
Here the reparameterisation makes the penalty the identity, so the block
needs no precision matrix and exactly one variance.
}
\keyword{internal}
