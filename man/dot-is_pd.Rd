% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{.is_pd}
\alias{.is_pd}
\title{Is a sparse symmetric matrix positive definite?}
\usage{
.is_pd(h)
}
\arguments{
\item{h}{A symmetric sparse matrix, or \code{NULL}.}
}
\value{
\code{TRUE}, \code{FALSE}, or \code{NA} if \code{h} is missing or has non-finite
entries, so the question cannot be put.
}
\description{
\code{Matrix::chol()} rather than \code{Matrix::Cholesky()}: the latter computes an
LDL' factorisation, which exists perfectly well for an indefinite matrix
and returns with nothing worse than a CHOLMOD warning, so it answers a
different question from the one being asked here. \code{chol()} fails on an
indefinite matrix, which is the answer wanted.
}
\keyword{internal}
