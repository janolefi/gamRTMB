% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design.R
\name{.build_design}
\alias{.build_design}
\title{Build the design for every distributional parameter}
\usage{
.build_design(par_formulas, data, parnames, knots = NULL)
}
\arguments{
\item{par_formulas}{List of one-sided formulas, one per parameter.}

\item{data}{Model frame.}

\item{parnames}{The family's modelled parameters.}

\item{knots}{Passed to \code{\link[mgcv:smoothCon]{mgcv::smoothCon()}}.}
}
\value{
A design object: per-parameter parts (parametric matrix, terms,
xlevels, smooths), the stacked fixed-effect matrices, index bookkeeping
into \code{beta} and \code{b}, the penalized blocks, and the variance-component
grouping.
}
\description{
Build the design for every distributional parameter
}
\section{Identifiability}{

\code{smoothCon(absorb.cons = TRUE)} applies the sum-to-zero constraint, so
smooths cannot collide with the intercept.
}

\section{Null spaces}{

Only the penalized blocks become random effects. The unpenalized
null-space columns are appended to that parameter's fixed-effect matrix and
are never pooled into the random-effect sum.
}

\section{Shared smoothing parameters}{

\code{s(..., id = )} does two things in mgcv and both are reproduced.
\emph{Linked bases}: the basis is built from the pooled covariate values of
the whole id group and then evaluated on each term's own data, via
\code{smoothCon(spec, pooled, n = nrow(data), dataX = data)}. This matters
beyond knot placement, because \code{scale.penalty} rescales each penalty by a
norm of its own model matrix; without pooling, two id-linked smooths get
different \code{S.scale} and one shared variance would mean different amounts of
smoothing for each. \emph{Shared smoothing parameter}: blocks in a group
get the same \code{sig_key}, which the engine turns into one variance component.

id groups are resolved globally across distributional parameters, so
\code{s(x, id = 1)} on \code{mu} and on \code{sigma} share one smoothness. A gam formula
cannot express this, having only one linear predictor.
}

\keyword{internal}
