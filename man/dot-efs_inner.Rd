% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efs.R
\name{.efs_inner}
\alias{.efs_inner}
\title{Inner solve: the coefficients at fixed smoothing parameters}
\usage{
.efs_inner(p, fn, gr, hfun, ctl, tag = "")
}
\arguments{
\item{p}{Coefficient vector. The smoothing parameters are not in it: they
reach the tape as data, and the caller has already put them where it
wants them.}
}
\value{
\code{list(p, f, g, converged, iter)}.
}
\description{
Maximises the penalized log-likelihood over \verb{c = (beta, b)} with
\code{log_sigma} held where the caller left it. Both are solved for, and both
are then integrated out by the criterion: \code{"aREML"} is the REML criterion,
so the mean-structure coefficients are random here exactly as they are
under \code{"REML"}.
}
\details{
A dispatcher over two solvers, neither of them written here:
\code{\link[=.efs_inner_bfgs]{.efs_inner_bfgs()}} is the default and \code{\link[=.efs_inner_newton]{.efs_inner_newton()}} is
\code{\link[TMB:newton]{TMB::newton()}}. Both of those topics record what was measured and why the
default is what it is.
}
\keyword{internal}
