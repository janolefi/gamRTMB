% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efs.R
\docType{data}
\name{.dense_hessian_cut}
\alias{.dense_hessian_cut}
\title{Predicted density above which the Hessian is treated as dense}
\format{
A single number in \verb{[0, 1]}.
}
\usage{
.dense_hessian_cut
}
\description{
Above this the penalized Hessian is read off an \code{\link[RTMB:TMB-interface]{RTMB::MakeADFun()}} rather
than off a composed second-order tape. Measured on the two routes at the
same point, same tape, every call at a \emph{different} argument -- repeating one
is served from a cache and reports a fraction of the true cost:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{  model                    H density   sparse tape      MakeADFun$he()
  film90, two s()             100\%   build 1.32s        build 0.046s
                                     eval  0.0767s      eval  0.0855s
  bs = "re", 400 levels       5.5\%   build 0.14s        build 0.02s
                                     eval  0.0078s      eval  0.2684s
}\if{html}{\out{</div>}}

Evaluation is a wash when the Hessian really is dense and 34 times better
for the tape when it is not, while the tape's \emph{build} -- which scales with
the nonzeros it has to produce -- costs 28 times more on the dense model
that gains nothing from it. So the tape is worth its build only where the
evaluations will be cheaper, and the crossover sits near half density.
}
\keyword{internal}
