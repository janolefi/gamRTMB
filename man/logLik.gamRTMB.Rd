% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{logLik.gamRTMB}
\alias{logLik.gamRTMB}
\title{Log-likelihood, and hence AIC and BIC}
\usage{
\method{logLik}{gamRTMB}(object, ...)
}
\arguments{
\item{object}{A \code{gamRTMB} fit.}

\item{...}{Ignored.}
}
\value{
An object of class \code{logLik}.
}
\description{
Returns the \strong{log-likelihood} of the data at the fitted
coefficients, with \code{df} set to the total effective degrees of freedom.
That is deliberately not the fitting criterion: under REML the criterion is
a restricted one, is not a likelihood, and is not comparable across
different mean structures, so using it for AIC would be wrong. It remains
available as \code{fit$objective}.
}
\details{
With \code{df} = total EDF this is the convention used by \code{\link[mgcv:gam]{mgcv::gam()}} and by
GAMLSS's GAIC, so \code{AIC()} and \code{BIC()} are comparable across models fitted
to the same response. Under \code{method = "ML"}, where effective degrees of
freedom are unavailable, \code{df} falls back to counting the fixed
coefficients and free smoothing parameters.
}
\examples{
set.seed(1)
d <- data.frame(x = runif(200)); d$y <- rnorm(200, sin(2 * pi * d$x), 0.3)
fit <- gamRTMB(y ~ list(mean = ~ s(x, k = 8)), data = d)
logLik(fit)
AIC(fit)
}
