% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efs.R
\name{.use_dense_hessian}
\alias{.use_dense_hessian}
\title{Whether to read the penalized Hessian off a dense object}
\usage{
.use_dense_hessian(design)
}
\arguments{
\item{design}{From \code{\link[=.build_design]{.build_design()}}.}
}
\description{
Three conditions, and all of them have to hold.
}
\details{
The Hessian has to be predicted dense (\code{\link[=.hessian_density]{.hessian_density()}}, against
\link{.dense_hessian_cut}), because that is what makes the sparse tape's
evaluations no cheaper. The problem has to be big enough that the tape's
build is worth avoiding (\link{.dense_hessian_min_work}), because on a small one
it is already cheap and \verb{$he()} is the slower of the two per call. And \code{p}
has to be small enough for a dense \eqn{p \times p} to be affordable at all
(\link{.efs_dense_max}) -- that last one is about memory rather than time, and
is a backstop rather than a case expected to arise, since a model with that
many coefficients and a genuinely dense Hessian has worse problems than
this choice.
}
\keyword{internal}
