% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R, R/methods.R
\name{gamRTMB}
\alias{gamRTMB}
\alias{nobs.gamRTMB}
\alias{coef.gamRTMB}
\alias{fitted.gamRTMB}
\title{Fit a distributional GAM}
\usage{
gamRTMB(
  formula,
  family = fam("norm"),
  data = NULL,
  weights = NULL,
  na.action = stats::na.omit,
  knots = NULL,
  method = c("REML", "ML", "aREML"),
  sigma_frac = 0.05,
  sparse = c("auto", "never", "always"),
  joint_precision = TRUE,
  start = NULL,
  silent = TRUE,
  control = list(),
  inner_control = list()
)

\method{nobs}{gamRTMB}(object, ...)

\method{coef}{gamRTMB}(object, ...)

\method{fitted}{gamRTMB}(object, ...)
}
\arguments{
\item{formula}{A two-sided formula whose right-hand side is a \code{list()} of
per-parameter formulas, or a plain right-hand side for the family's first
parameter.}

\item{family}{A \code{gamRTMB_family}, from \code{\link[=fam]{fam()}}. See \code{\link[=families]{families()}}.}

\item{data}{A data frame holding every model variable, or \code{NULL} (the
default) to take them from the environment of \code{formula}.}

\item{weights}{Optional prior weights, evaluated in \code{data}. As in
\code{\link[stats:glm]{stats::glm()}}, each observation's log-density contribution is multiplied
by its weight.}

\item{na.action}{How to treat missing values in any model variable;
\code{\link[stats:na.fail]{stats::na.omit()}} by default, which drops those rows and reports how
many in the fit's summary.}

\item{knots}{Passed to \code{\link[mgcv:smoothCon]{mgcv::smoothCon()}}.}

\item{method}{Smoothness selection criterion: \code{"REML"} (default), \code{"ML"},
or \code{"aREML"} for approximate REML by extended Fellner-Schall. See the
Smoothness selection section, which says when the third is worth
reaching for and what it costs.}

\item{sigma_frac}{Tuning constant for the variance-component starting
values: each smooth starts contributing this fraction of its parameter's
linear-predictor scale. See \code{\link[=.init_pars]{.init_pars()}}. Raise it if a fit converges
to an over-smooth solution. If the objective is not finite here, a few
other values are tried automatically before giving up (see
\link{.sigma_frac_ladder}) and the one used is reported; passing this
argument explicitly does not switch that off, but passing \code{start} does.}

\item{sparse}{How to treat a smooth whose single penalty is already sparse
-- a Markov random field, a random walk, a supplied GMRF precision.
\code{"auto"} (default) keeps the penalty and gives the block a
\code{\link[RTMB:MVgauss]{RTMB::dgmrf()}} prior when it has at least 50 coefficients and is at
most 20\% nonzero, and sends everything else through
\code{\link[mgcv:smooth2random]{mgcv::smooth2random()}} as usual. \code{"never"} is the old behaviour;
\code{"always"} takes the sparse route for every single-penalty smooth, which
is mainly useful for checking that the two agree. See \code{\link[=.gmrf_block]{.gmrf_block()}}.}

\item{joint_precision}{Ask \code{\link[RTMB:TMB-interface]{RTMB::sdreport()}} for the joint precision
matrix, which \code{\link[=predict]{predict()}} needs for standard errors. On by default; turn
it off to save time and memory on large models where bands are not
wanted.}

\item{start}{Optional named list overriding entries of the starting
parameter list (\code{beta}, \code{b}, \code{log_sigma}).}

\item{silent}{Under \code{"REML"} and \code{"ML"}, passed to \code{\link[RTMB:TMB-interface]{RTMB::MakeADFun()}}.
Under \code{"aREML"} there is no such object, so it means the same thing
directly: \code{silent = FALSE} prints one line per outer iteration -- the
criterion, its change, the largest Fellner-Schall gradient component, the
accepted step length and the smoothing parameters on the scale \code{\link[=edf]{edf()}}
reports -- plus the model's size before the first inner solve and why the
iteration stopped.

\code{control = list(trace = 2)} adds one line per \emph{inner} Newton step, which
is what to reach for when a fit is slow: on the harder families the outer
iterations are not where the time goes, and a single inner solve running
to its iteration cap looks identical from outside to a fit that has
hung. \code{control = list(trace = )} overrides \code{silent} either way.}

\item{control}{Under \code{"REML"} and \code{"ML"}, passed to \code{\link[stats:nlminb]{stats::nlminb()}}.
Under \code{"aREML"}, merged onto \link{.efs_defaults}: \code{maxit}, \code{tol}, \code{gtol},
\code{max_step}, \code{max_halve}, \code{stall_tol}, \code{trace} (0 silent, 1 outer
iterations, 2 inner as well), and \code{inner_method}. The last is \code{"bfgs"} by
default, which is what makes the harder four-parameter families fit at
all; \code{"newton"} is \code{\link[TMB:newton]{TMB::newton()}}, two to three times faster on terms
with many coefficients and an ordinary family -- a Markov random field,
an SPDE mesh -- and not to be used otherwise. See \code{\link[=.efs_inner_bfgs]{.efs_inner_bfgs()}} for
what was measured.}

\item{inner_control}{Passed to \code{\link[RTMB:TMB-interface]{RTMB::MakeADFun()}}'s \code{inner.control}, which
governs the inner Newton solve over the coefficients rather than the
outer optimisation of the smoothing parameters. \code{list(maxit = ...)} is
the entry worth reaching for; see \code{dev/NOTES-inner-method.md} for why
\code{inner.method} is not exposed.}

\item{object}{A \code{gamRTMB} fit.}

\item{...}{Ignored.}
}
\value{
An object of class \code{gamRTMB}.
}
\description{
Smooth terms on every parameter of a distribution, fitted by combining
\pkg{mgcv}'s basis and penalty construction with \pkg{RTMB}'s automatic
differentiation and Laplace approximation, over the log-densities in
\pkg{RTMBdist}.
}
\section{Methods (by generic)}{
\itemize{
\item \code{nobs(gamRTMB)}: Number of observations actually used.

\item \code{coef(gamRTMB)}: Coefficients, as a list of the fixed (\code{beta}) and
penalized (\code{b}) vectors plus the log variance components. \code{beta} and
\code{log_sigma} are named \code{parameter:term}.

\item \code{fitted(gamRTMB)}: Fitted values of every distributional parameter, on the
response scale.

}}
\section{Formula}{

The response is the left-hand side of the outer formula and each
distributional parameter gets a one-sided formula:
\code{y ~ list(mean = ~ s(x1) + s(x2), sd = ~ s(x1))}. Parameters the family
declares but the formula omits are given \code{~1}. Parameter names are the
density's own (\code{xi}, \code{omega}, \code{alpha} for a skew normal), not generic
location/scale/shape labels.

A plain right-hand side is shorthand for modelling the family's \emph{first}
parameter and leaving the rest constant, so \code{y ~ s(x)} means
\code{y ~ list(mean = ~ s(x))} for \code{fam("norm")}. That is the ordinary gam
formula, and it is what makes the one-parameter case read like one.
}

\section{Data}{

\code{data} is optional. Without it the model variables are looked up where the
formula was written, as in \code{\link[stats:glm]{stats::glm()}} or \code{\link[mgcv:gam]{mgcv::gam()}}; they are
collected into a data frame first, so everything downstream — \code{na.action},
\code{\link[=predict]{predict()}}, the plots — sees the same rectangle either way. Variables
found this way must all have the same length.
}

\section{Sparse penalties}{

A smooth whose penalty is a sparse precision matrix -- \code{bs = "mrf"} over an
adjacency graph, a random walk, or any precision supplied through
\code{xt = list(penalty = )} -- can skip \code{\link[mgcv:smooth2random]{mgcv::smooth2random()}}. That rotation
makes the coefficients iid, which is convenient but fills the penalty in
completely; keeping it instead and giving the block an
\eqn{N(0, \sigma^2 Q^{-1})} prior through \code{\link[RTMB:MVgauss]{RTMB::dgmrf()}} is the same
model at a fraction of the cost. The \code{sparse} argument controls this.

An intrinsic field is corner-constrained rather than sum-to-zero
constrained, since the latter is what destroys the sparsity. The two are
equivalent up to a constant absorbed by the intercept, so such a term needs
its parameter to have one. See \code{\link[=.null_space]{.null_space()}}.
}

\section{Smoothness selection}{

Three criteria, and the third is the reason the other two are named rather
than assumed.

\code{"REML"} (the default) puts the mean-structure coefficients into the random
vector alongside the spline coefficients, so the same Laplace approximation
integrates out both. This is the bias and stability correction of Wood
(2011); it is not a sparsity argument, since mgcv's bases have global
support and are dense either way.

\code{"ML"} keeps them as fixed effects, which also means \code{\link[=edf]{edf()}} is
unavailable, since the penalized Hessian over every coefficient is not
formed.

\code{"aREML"} is \strong{approximate REML}: the same criterion as \code{"REML"},
optimised by the extended Fellner-Schall method of Wood & Fasiolo (2017)
rather than by handing the smoothing parameters to \code{nlminb}. The two agree
closely -- over the models in \code{dev/bench-efs.R}, within a few thousandths
of a nat on the criterion and about a tenth of an effective degree of
freedom -- and the approximation is in the \emph{gradient}, not the criterion.

The distinction is worth stating because it decides when to reach for it.
Under \code{"REML"} the outer gradient differentiates \eqn{\log|H|} with respect
to the smoothing parameters, which needs third derivatives of the
log-likelihood in the coefficients: the expensive term, in both time and
tape memory, and the one that requires the inner Hessian to be positive
definite. Fellner-Schall drops exactly that term and replaces the gradient
step with a multiplicative update. For the saving to be real nothing may be
declared random, so \code{"aREML"} owns its own inner solve as well as the outer
update; see \code{\link[=.fit_efs]{.fit_efs()}}.

\strong{What \code{"aREML"} is for} is models \code{"REML"} cannot fit at all. It never
needs the inner Hessian to be positive definite -- it repairs the data
Hessian instead -- and that is where \code{"REML"} fails outright on the harder
four-parameter families, \code{bcpe} and its relatives. On those it is the only
one of the three that returns a fit.

It is \emph{not}, at the sizes measured so far, reliably faster on models where
both work: over eight of them it ranged from 0.5 to 2.4 times \code{"REML"}'s
time with no clear pattern, because the third-derivative term it avoids is
not yet the dominant cost on a few hundred observations.

Three things to know before using it. \code{max_grad} is a diagnostic rather
than a stationarity certificate, since the Fellner-Schall gradient does not
reach zero at the optimum. The coefficient covariance conditions on the
fitted smoothing parameters instead of allowing for their uncertainty, so
standard errors and bands are a little narrow; see \code{\link[=vcov.gamRTMB]{vcov.gamRTMB()}}. And
\code{sigma_frac}, \code{joint_precision} and \code{inner_control} do not apply.
}

\section{Supported smooths}{

\code{s()}, \code{t2()}, \verb{by =} variables, \code{bs = "fs"} and \code{bs = "re"} all
reconstruct exactly through \code{\link[=.reconstruct_map]{.reconstruct_map()}}. \code{te()} is not supported,
because mgcv itself declines \code{smooth2random(type = 2)} for it and directs
you to \code{t2()}. \code{fx = TRUE} is rejected, having no penalized part.
\code{s(..., id = )} shares one smoothing parameter across a group of smooths,
including across distributional parameters.
}

\examples{
set.seed(1)
d <- data.frame(x1 = runif(200), x2 = runif(200))
d$y <- rnorm(200, sin(2 * pi * d$x1), exp(-1 + d$x2))
fit <- gamRTMB(y ~ list(mean = ~ s(x1, k = 8), sd = ~ s(x2, k = 8)),
               data = d)
fit
edf(fit)

## a plain right-hand side models the family's first parameter
gamRTMB(y ~ s(x1, k = 8), data = d)
}
