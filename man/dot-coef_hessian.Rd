% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{.coef_hessian}
\alias{.coef_hessian}
\title{The penalized Hessian in the coefficients, from a tape of its own}
\usage{
.coef_hessian(nll, beta, b, log_sigma)
}
\arguments{
\item{nll}{The joint penalized negative log-likelihood, from
\code{\link[=.make_nll]{.make_nll()}} with \code{obs = FALSE}.}

\item{beta, b}{The fitted coefficients.}

\item{log_sigma}{The fitted log standard deviations, at full length. Held
constant: the tape is over the coefficients alone, which also keeps a
\code{dgmrf} block's \verb{log|Q(theta)|} off it, since RTMB declines a second
sparse Jacobian through one.}
}
\value{
A symmetric sparse matrix ordered \code{beta} then \code{b}, or \code{NULL} if the
tape could not be built.
}
\description{
The curvature of the joint penalized negative log-likelihood in
\code{c(beta, b)}, at fixed smoothing parameters. Used under \code{method = "ML"},
where it cannot be had from the fitted object: \code{RTMB::MakeADFun()} builds
its sparse Hessian with \code{skipFixedEffects}, so what comes back covers the
random vector only and leaves the \code{beta} rows empty. Under \code{"ML"} the
unpenalized coefficients are not in that vector, and the matrix would be
missing exactly the rows \code{\link[=edf]{edf()}} needs.
}
\details{
Nothing about the quantity depends on the criterion, though. It is the
second derivative of the same penalized objective either way, so it is
taped here rather than declared unavailable.

The \code{nll} must be one built with \code{obs = FALSE}. \code{RTMB::OBS()} keys on the
deparsed name of its argument in a registry global to RTMB, which
\code{MakeADFun} resets per object and \code{MakeTape} does not -- so a tape built
over an \code{obs = TRUE} objective leaves \code{"y"} behind pointing at this
model's response, and the next model's \code{OBS(y)} returns it. See
\code{\link[=.make_nll]{.make_nll()}}.
}
\keyword{internal}
