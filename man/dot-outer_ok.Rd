% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{.outer_ok}
\alias{.outer_ok}
\title{Whether the outer optimisation got there, by code or by gradient}
\usage{
.outer_ok(opt, max_grad)
}
\arguments{
\item{opt}{The \code{nlminb} result.}

\item{max_grad}{Its largest absolute outer gradient component, from
\code{\link[=.outer_grad]{.outer_grad()}}.}
}
\value{
\code{TRUE} or \code{FALSE}.
}
\description{
\code{nlminb}'s convergence code on its own is the wrong test on this criterion.
The REML surface in the smoothing parameters has genuinely flat directions
-- \code{\link[=.theta_bounds]{.theta_bounds()}} is the box that stops a fit walking down one -- and
\code{nlminb} answers a flat minimum with \verb{false convergence (8)} and a nonzero
code however small the gradient it is sitting on. On \code{MASS::mcycle} with a
t response and \code{sigma_frac = 0.001} it stops at \verb{max|g| = 1.9e-04}, which
is converged by any standard, and that fit was reported as a failure.
}
\details{
So a nonzero code is forgiven when the outer gradient is small. The
threshold is \link{.efs_defaults}'s \code{gtol}, so that the two engines mean the
same thing by the word; if anything it is the stricter test here, since
this gradient is exact where the Fellner-Schall one drops a
third-derivative term.

The converse is deliberately not done: a zero code is never overruled by a
large gradient. \code{nlminb} reporting a relative reduction it cannot improve
on is its own evidence, and on a criterion this flat the gradient is the
weaker of the two signals.
}
\keyword{internal}
