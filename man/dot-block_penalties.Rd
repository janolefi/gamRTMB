% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/efs.R
\name{.block_penalties}
\alias{.block_penalties}
\title{A block's penalty matrices and their exponents}
\usage{
.block_penalties(bl)
}
\arguments{
\item{bl}{A block from \code{\link[=.build_design]{.build_design()}}.}
}
\value{
\code{list(S, A)}: a list of \code{nS} sparse penalty matrices, and an \code{nS}
by \code{bl$ntheta} matrix of exponent coefficients.
}
\description{
The engine's view of a penalized block, and the only place that knows how
a block's parameters turn into a precision for a \emph{numeric} theta:
\deqn{Q_k(\theta) = \sum_i \exp(A_{i\cdot}\theta) S_{ki}.}
\code{\link[=.block_prec]{.block_prec()}} is the same statement written for the AD tape, where the
switch on \code{kind} has to stay so that \code{dnorm} and \code{dgmrf} see the shapes
they expect. The two must agree; \code{test-efs.R} checks that they do.
}
\details{
The \code{A} matrix is mgcv's \code{L} convention generalised to every block:
\verb{\eqn{\lambda = \exp(L\theta)}}. An \code{"iid"} or \code{"gmrf"} block has one
penalty matrix and one parameter, the log standard deviation, so
\eqn{\lambda = \exp(-2\theta)} and \code{A} is the 1 by 1 matrix \code{-2}.
}
\keyword{internal}
