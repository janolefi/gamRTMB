% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmrf.R
\name{.penalty_spec}
\alias{.penalty_spec}
\title{How a block's parameters become its penalty}
\usage{
.penalty_spec(sm, Sk)
}
\arguments{
\item{sm}{The \code{smoothCon} object.}

\item{Sk}{Its penalties, already reduced to the retained coefficients.}
}
\description{
The prior on every penalized block is
\deqn{Q_k(\theta) = \sum_i \exp(A_{i\cdot}\theta)\, S_{ki},}
and \code{A} -- a linear map from the parameters actually estimated to the log
penalty weights -- is the only thing that differs between kinds of smooth.
mgcv writes it \code{L}, for the blocks it has a name for. This is where a
smooth's penalties are turned into that description; \code{\link[=.block_prec]{.block_prec()}} and
\code{\link[=.block_penalties]{.block_penalties()}} are the two readings of it, on the tape and off.
}
\details{
\tabular{llll}{
   penalties \tab \eqn{S_i} \tab \code{A} \tab \eqn{\theta} \cr
   one, no \code{L} \tab the penalty itself \tab \eqn{-2} \tab \eqn{\log\sigma} \cr
   several, with \code{L} \tab \eqn{C, G_1, G_2} \tab mgcv's \code{L} \tab \eqn{(\log\sigma, \log\rho)} \cr
   several, no \code{L} \tab \eqn{S_1 \ldots S_m} \tab \eqn{I_m} \tab \eqn{\log\lambda} \cr
}


All three are the same \code{"multi"} block. A single sparse penalty scaled by a
variance is not a different kind of object from several combined through an
\code{L}; it is that object with \eqn{L = [-2]} and one matrix, and giving it a
kind of its own bought a branch in every function that switches on one.
Only \code{"iid"} is genuinely apart, and only because its penalty is the
identity: it needs no matrix at all, and gets \code{dnorm} rather than \code{dgmrf}.
}
\section{Reporting and starting values}{

\code{sp_pow} records the diagonal of \code{A} when \code{A} is diagonal, so that one
penalty weight belongs to one parameter and can be reported as
\eqn{\lambda_i = \exp(sp\_pow_i\,\theta_i)}. An SPDE's \code{L} is not
diagonal -- three matrices, two parameters -- so there is no such
\eqn{\lambda} to report and its own named parameters are shown instead;
\code{sp_pow} is \code{NULL} there and that is what says so.

The same field carries the starting values. Scaling a whole block's penalty
by \eqn{\sigma^{-2}} is a shift of \eqn{-2/sp\_pow} in \eqn{\theta},
which is \eqn{+1} for a log standard deviation and \eqn{-2} for a log
penalty weight, so \code{\link[=.init_pars]{.init_pars()}}'s one rule covers both without knowing
which it has. A block whose \code{L} is not diagonal keeps the absolute
\code{theta.start} its constructor chose.

\code{qscale} is the matching piece: a typical diagonal entry of the penalty at
\eqn{\theta = 0}, which is what turns \eqn{\sigma} into the term's implied
prior standard deviation. Balanced across penalties, for the reason in
\code{\link[=.balanced_sum]{.balanced_sum()}}.
}

\keyword{internal}
