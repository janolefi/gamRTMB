# The emptiest corner, for a legend

Counts what is drawn in each corner of the plotting region and returns
the least crowded, so a five-curve legend does not land on top of the
curves. `xs` and `ys` must be matched coordinates of everything drawn.

## Usage

``` r
.empty_corner(xs, ys, yl)
```
