# Colour-blind safe palette

Okabe-Ito, in base R since 4.0, so no dependency. Used only where series
are overlaid; a single-series plot needs no colour at all. The pale
yellow is dropped: it is fine for fills but too low-contrast for a line
on white.

## Usage

``` r
.pal(n)
```
