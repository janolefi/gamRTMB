# Resolve a family's fixed arguments against the data

A numeric constant is recycled; a character string or one-sided formula
names a column of `data`.

## Usage

``` r
.resolve_fixed(family, data, n)
```
